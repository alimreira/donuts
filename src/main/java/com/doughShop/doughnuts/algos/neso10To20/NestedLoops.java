package com.doughShop.doughnuts.algos.neso10To20;

public class NestedLoops {
    public static void main(String[] args) {
        /*
        * Nested loop consist of an outer loop and one or more inner loops
        * Each time an outer loop is executed, the inner loops are executed again
        * When the inner loop finishes executing, we continue to execute the body of the outer loop.
        * Each time we execute the outer loop, we re-execute the inner loop from the beginning
        * **/


    }
}
